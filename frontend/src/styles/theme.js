import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#FAF9F6'
        },
        secondary: {
            main: '#93CBC4'
        }
    },
    typography: {
        button: {
            color: '#4B4737'
        }
    }
});

export default theme;